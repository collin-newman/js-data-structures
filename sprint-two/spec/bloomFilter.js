describe('bloomFilter', function() {

  beforeEach(function() {
    bloomFilter = new BloomFilter(24);
  });

  it('should not iterate thru the entire data set when searching', function() {

  });

});